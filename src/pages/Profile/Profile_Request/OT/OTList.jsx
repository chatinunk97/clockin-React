export default function OTList({ xxxx }) {
    return (
        <div className='flex flex-col gap-4 justify-center items-center'>
            {.map(el => <xxxxx key={el.id} xxxx={el} />)}
        </div>
    )
}