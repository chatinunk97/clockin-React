import InfoClockinItem from "./InfoClockinItem";

export default function InfoClockin() {
    return (
        <div className="">
            {xxxxx.map(el => <InfoClockinItem key={el.id} xxxxx={el} />)}
        </div>
    )
}
